{
  "name": "@cockpit-xp-breakout/sender-board",
  "scripts": {
    "build": "pio run -e esp",
    "build:i2c": "pio run -e esp-i2c",
    "upload": "pio run --target upload -e esp && pio device monitor",
    "upload:i2c": "pio run --target upload -e esp-i2c && pio device monitor",
    "monitor": "pio device monitor"
  },
  "private": true,
  "devDependencies": {
    "rimraf": "^5.0.7"
  }
}