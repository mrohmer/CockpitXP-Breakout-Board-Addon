import {demo} from '$lib/api/demo';
import {flags} from '$lib/api/flags';
import { pitlanes } from './pitlanes';
import {state} from '$lib/api/state';

export const api = {
  demo,
  pitlanes,
  flags,
  state,
}
