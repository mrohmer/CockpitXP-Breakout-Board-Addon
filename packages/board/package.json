{
  "name": "@rohmer/cockpit-xp-breakout-board-board",
  "scripts": {
    "build:esp": "pio run -e nodemcuv2",
    "build:esp:test": "pio run -e nodemcuv2-test",
    "upload:esp": "pio run --target upload -e nodemcuv2 && pio device monitor",
    "upload:esp:test": "pio run --target upload -e nodemcuv2-test && pio device monitor",
    "build:arduino": "pio run -e arduino",
    "build:arduino:test": "pio run -e arduino-test",
    "upload:arduino": "pio run --target upload -e arduino && pio device monitor",
    "upload:arduino:test": "pio run --target upload -e arduino-test && pio device monitor",
    "monitor": "pio device monitor"
  },
  "private": true
}
